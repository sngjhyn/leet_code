class Solution:
    def canConstruct(self, ransomNote, megazine):
        st1, st2 = Counter(ransomNote), Counter(megazine)
        if st1 & st2 == st1:
            return True
        return False
        
